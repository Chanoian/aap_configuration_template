---
all:
  children:
    dev:
      hosts:
        127.0.0.1: # Due to the way this template is configured, DO NOT CHANGE THIS!
      vars:
        connection: local

#    automationhub:
#      hosts: aap-hub-aap.apps.cluster-fwxnj-1.dynamic.redhatworkshops.io

#    automationedacontroller:
#      hosts: aap-eda-aap.apps.cluster-fwxnj-1.dynamic.redhatworkshops.io
  
#    automationcontroller:
#      hosts: aap-controller-aap.apps.cluster-fwxnj-1.dynamic.redhatworkshops.io

 #   automationedacontroller:
 #     hosts: aap-aap.apps.cluster-fwxnj-1.dynamic.redhatworkshops.io

#    builder:
#      hosts:
#        HERE: # FQDN of builder host if you have one. Otherwise, can be Automation Hub host or commented out if not in use.

    # Only needed if installing AAP with automation, can be removed if you are not. See Install docs if unsure what server should be the database host.
#    database:
#      hosts:
#        HERE:
  vars:
    env: dev
    aap_host: aap-aap.apps.cluster-fwxnj-1.dynamic.redhatworkshops.io
    aap_pass: XXXXXXXXXXXX
    aap_user: admin
...
